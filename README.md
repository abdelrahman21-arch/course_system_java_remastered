# course_system

Minimal Spring Boot project scaffolded with Maven.

## Run

Once Maven is available on your machine, run:

```powershell
mvn spring-boot:run
```

The app starts on `http://localhost:8080/`.
